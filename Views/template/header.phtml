<?php

if (empty($_SESSION['csrf_token'])) {
    $_SESSION['csrf_token'] = bin2hex(random_bytes(32));
}
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Bootstrap core CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link href="/css/my-style.css" rel="stylesheet">
    <!-- Leaflet CSS -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />

    <!-- Leaflet JS -->
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

    <script src="/js/map.js"></script>
    <title>Server-Side Programming - <?php echo isset($view->pageTitle) ? $view->pageTitle : 'Home'; ?></title>

    <style>
        /* Center the navbar content */
        .navbar-nav {
            margin-left: auto;
        }

        .nav-item {
            position: relative;
        }

        /* Hover effect for the links */
        .nav-link:hover {
            color: #f8f9fa; /* Change the hover color to a lighter shade */
            background-color: #007bff; /* Add a background color on hover */
            border-radius: 4px; /* Slight border-radius for the hover effect */
        }

        .navbar-brand {
            font-weight: bold; /* Make the brand name bold */
            font-size: 1.5rem;
        }

        /* Add some padding to the navbar items */
        .navbar-nav .nav-item {
            padding-left: 15px;
            padding-right: 15px;
        }

        /* Active navbar item style */
        .nav-link.active {
            color: #007bff;
            font-weight: bold;
        }
    </style>
</head>

<body class="d-flex flex-column h-100">

<header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark shadow-lg">
        <div class="container-fluid">
            <!-- Left side of the navbar (ecoBuddy branding) -->
            <a class="navbar-brand" href="index.php">ecoBuddy</a> <!-- ecoBuddy clickable to index.php -->

            <!-- Toggle button for mobile view -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Navbar items that collapse on small screens -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="browse.php">Search</a> <!-- Search button redirects to browse.php -->
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="browse.php">Browse</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="login.php">Login</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

<!-- Bootstrap core JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>

</body>
</html>
